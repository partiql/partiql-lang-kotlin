case::{
  input: "order_by-01",
  catalog: "default",
  schema: {
    type: "list",
    items: {
      type: "struct",
      fields: [
        {
          name: "id",
          type: "int"
        },
        {
          name: "breed",
          type: "string"
        }
      ],
      constraints: [
        closed,
        unique,
        ordered
      ]
    }
  }
}

case::{
  input: "order_by-02",
  catalog: "default",
  schema: {
    type: "list",
    items: {
      type: "struct",
      fields: [
        {
          name: "id",
          type: "int"
        },
        {
          name: "breed",
          type: "string"
        }
      ],
      constraints: [
        closed,
        unique,
        ordered
      ]
    }
  }
}

case::{
  input: "order_by-03",
  catalog: "default",
  schema: {
    type: "list",
    items: {
      type: "struct",
      fields: [
        {
          name: "id",
          type: "int"
        },
        {
          name: "breed",
          type: "string"
        }
      ],
      constraints: [
        closed,
        unique,
        ordered
      ]
    }
  }
}
