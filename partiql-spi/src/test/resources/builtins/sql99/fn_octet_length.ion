{
  name: octet_length,
  type: fn,
  description:'''
    Placeholder for `octet_length` description
  ''',
  properties: [],
  impls: [
    // character strings
    (fn string::char -> int),
    (fn string::varchar -> int),
    (fn string::string -> int),
    // character large object
    (fn string::clob -> int),
    // bit strings?
    (fn string::blob -> int),
  ]
}
