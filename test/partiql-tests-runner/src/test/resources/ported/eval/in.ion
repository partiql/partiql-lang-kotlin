
{
  name:"0 IN (1, 2, 3)",
  statement:"0 IN (1, 2, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:false
  }
}
{
  name:"1 IN (1, 2, 3)",
  statement:"1 IN (1, 2, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:true
  }
}
{
  name:"2 IN (1, 2, 3)",
  statement:"2 IN (1, 2, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:true
  }
}
{
  name:"3 IN (1, 2, 3)",
  statement:"3 IN (1, 2, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:true
  }
}
{
  name:"4 IN (1, 2, 3)",
  statement:"4 IN (1, 2, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:false
  }
}
{
  name:"0 IN (1, 1+1, 3)",
  statement:"0 IN (1, 1+1, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:false
  }
}
{
  name:"1 IN (1, 1+1, 3)",
  statement:"1 IN (1, 1+1, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:true
  }
}
{
  name:"2 IN (1, 1+1, 3)",
  statement:"2 IN (1, 1+1, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:true
  }
}
{
  name:"3 IN (1, 1+1, 3)",
  statement:"3 IN (1, 1+1, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:true
  }
}
{
  name:"4 IN (1, 1+1, 3)",
  statement:"4 IN (1, 1+1, 3)",
  assert:{
    result:EvaluationSuccess,
    evalMode:[
      EvalModeCoerce,
      EvalModeError
    ],
    output:false
  }
}
{
  name:"1 IN 'so long'",
  statement:"1 IN 'so long'",
  assert:[
    {
      result:EvaluationSuccess,
      evalMode:EvalModeError,
      output:false
    },
    {
      result:EvaluationSuccess,
      evalMode:EvalModeCoerce,
      output:false
    }
  ]
}